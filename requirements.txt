# Core dependencies for Synthetic A/B Experimenter
streamlit>=1.28.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# LLM integration
groq>=0.4.0
openai>=1.0.0

# Standard library dependencies (already included in Python)
# json, math, random, time, re, os, dataclasses, typing

